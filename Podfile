# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NewPorject' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Cupcake'
  pod 'Moya'
  pod 'SwiftyJSON'
  pod 'Kingfisher'
  pod 'MJRefresh'
  # Pods for NewPorject

end
